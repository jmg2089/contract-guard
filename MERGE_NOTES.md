# 프론트 병합 기록 — 무엇을 합쳤고, 무엇을 고쳤는가

강수현 님 프론트와 백엔드를 합친 내용입니다. 강수현 님께 그대로 전달하셔도 됩니다.

---

## 1. 먼저 알아야 할 것 — 결과 화면이 가짜였습니다

받은 프론트에는 **판정 결과를 화면에 그리는 코드가 없었습니다.**

```js
window.contractAnalysisResult = result;   // 받아서 넣어는 두는데
                                          // 이걸 읽는 코드가 어디에도 없음
```

서버를 정상으로 호출하고 결과도 제대로 받아오는데, 그 결과를 버리고
HTML에 손으로 써 둔 예시(제7조 위약금 300만원, 제9조 연차 10일, 제10조 수습 6개월)를
그대로 보여주고 있었습니다.

**어떤 계약서를 올려도 같은 화면이 나옵니다.** 심사위원이 자기 계약서를 넣는 순간
드러납니다. 디자인 작업 중 당연히 남을 수 있는 상태이고, 이번에 그 자리를 채웠습니다.

---

## 2. 새로 만든 것

### `static/js/render.js`

서버 응답을 강수현 님 마크업에 채워 넣는 층입니다. **클래스 이름을 하나도 바꾸지
않았습니다** — `style.css` 가 그대로 먹어야 하기 때문입니다.

| 채우는 곳 | 내용 |
|---|---|
| `.summary-message h2` | "11개 조항 중 6개 조항을 확인해 주세요" |
| `.summary-numbers strong` | 전체 / 확인 필요 / 특이사항 없음 / 검증 제외 |
| `#verificationBanner` | 인용 검증에서 걸러진 건수 (0건이면 통과했다고 표시) |
| `.analysis-list` | 판정 카드 — `.analysis-card` 구조 그대로 |
| `[data-filter]` | 필터 버튼의 개수 |

조 번호가 없는 계약서(표·번호 목록형)도 많아서, 카드 제목은 `임 금 : 월 160만원` 같은
본문이 통째로 들어가지 않도록 항목명만 뽑습니다.

### 서버 라우트 두 개

프론트가 부르는데 백엔드에 없던 주소를 추가했습니다.

- `POST /api/analyze-pdf` — PDF 업로드 → 글자 추출 → 판정까지 한 번에
- `POST /api/contact` — 문의 메일 (아래 4번)

---

## 3. 합치면서 들어온 백엔드 기능

프론트 쪽에는 없던 층들입니다. 화면에도 연결했습니다.

**분석할 수 없는 입력은 결과 화면으로 보내지 않습니다.**
근로계약서가 아닌 문서(수료증·이력서·용역계약서)에 "0건 · 안전"을 띄우면,
검사하지 않았다는 사실이 '문제 없음'으로 읽힙니다. 법률 도구에서 가장 위험한 오해입니다.
`Render.blocked()` 가 이걸 막고 이유를 안내합니다.

**민감정보 마스킹.** 이름·주민등록번호·계좌번호 등은 검사 전에 브라우저에서 가려
서버로 보내지 않습니다. 결과 화면에 몇 건을 가렸는지 표시합니다.

**PDF 마스킹 미리보기.** 문서 모양 그대로 개인정보 자리에 검은 박스가 덮인 그림을
보여줍니다. 지금은 `templates/index_simple.html` 쪽에만 붙어 있습니다 —
강수현 님 업로드 화면(`#dropZone`)에 붙이려면 파일 선택 직후
`/api/mask-preview` 를 호출하고 반환된 `pages[].image` 를 `<img>` 로 띄우면 됩니다.

---

## 4. 문의 메일 — `app.py` → `core/contact.py`

기능과 응답 형식(`{success, message}`)은 그대로입니다. **세 가지만 고쳤습니다.**

1. **SMTP 계정과 비밀번호 길이를 `print` 로 찍던 부분을 지웠습니다.**
   Render 로그에 그대로 쌓이고, 로그는 우리가 지운다고 사라지지 않습니다.
2. **보내는 주소에 개인 메일이 기본값으로 박혀 있던 것**(`wendy132@naver.com`)을
   환경변수로 뺐습니다. 코드에 박히면 `.gitignore` 와 무관하게 깃에 올라갑니다.
3. 오타로 중복돼 있던 블록(`afe_name`)을 정리했습니다.

설정은 `.env` 에 넣습니다. 비워 두면 문의 기능만 안 되고 나머지는 정상입니다.

```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
CONTACT_RECEIVER=
```

`CONTACT_RECEIVER` 는 **우리 쪽 고정 주소**여야 합니다. 사용자가 입력한 주소로 보내는
구조면 스팸 발송 도구로 악용될 수 있습니다.

문의 폼에 넣을 안내 문구는 `PRIVACY_NOTES.md` 3번에 있습니다. 문의창에 계약서를
붙여넣으면 이메일로 나가서 우리가 회수할 수 없기 때문에, 그러지 않도록 안내가 필요합니다.

---

## 5. 서버는 하나입니다

`app.py` 는 더 이상 쓰지 않습니다. `server.py` 하나로 돕니다.
두 개를 띄우면 포트가 갈리고 배포 설정도 갈라집니다.

```
python server.py
```

`Procfile` 과 `render.yaml` 도 `server:app` 을 가리킵니다.

---

## 6. 확인 방법

```
python scripts/doctor.py      # 환경 점검
python -m eval.run_eval --no-llm
python server.py
```

`http://127.0.0.1:5000` 접속 후 `static/samples/test_with_privacy.pdf` 업로드.
민감정보 13건이 가려지고, 위반 6건이 실제 판정으로 표시되어야 합니다.
**제7조 위약금 300만원 같은 예시 카드가 보이면 `render.js` 가 안 붙은 것입니다.**

---

## 7. 아직 안 된 것

- 마스킹 미리보기를 강수현 님 업로드 화면에 연결 (위 3번)
- 스캔본·사진의 OCR 좌표 마스킹 — 실기기 검증이 필요해 미뤘습니다
- `#clauseSearch` 검색과 `[data-filter]` 필터는 기존 동작 그대로입니다.
  새로 그린 카드에도 `data-search`, `data-status` 를 넣어 두었으니 그대로 걸립니다.
