# 개인정보 — 무엇을 했고, 프론트에서 무엇이 필요한가

강수현 님께 전달할 내용입니다. 문의 폼에 넣을 문구는 3번에 그대로 복사할 수 있게 적어 뒀습니다.

---

## 1. 결론부터

`.gitignore` 추가만으로는 개인정보 문제가 해결되지 않습니다. 셋은 서로 다른 문제입니다.

| 문제 | 상태 |
|---|---|
| API 키가 깃에 올라가는 것 | **해결.** `.gitignore` 보강 + 히스토리 확인 결과 커밋된 적 없음 |
| 계약서 본문이 서버·외부로 나가는 것 | **해결.** 저장 안 함 · 외부 API 0회 · 민감정보는 전송 전 마스킹 |
| 문의 이메일로 개인정보가 나가는 것 | **부분 해결.** 구조상 회수 불가 → 안내로 막아야 함 (3번) |

깃 히스토리 확인은 이렇게 했습니다. 결과는 `.env.example` 하나뿐이라 키 재발급이 필요 없었습니다.

```
git log --all --full-history -- .env .env.save
git ls-files | findstr /i env
```

---

## 2. 이번에 추가한 것

### 민감정보 자동 마스킹

검사에 쓰이지 않는 정보를 **브라우저에서 먼저** 가린 뒤 서버로 보냅니다.
위약금 조항이 위법인지 따지는 데 주민등록번호는 한 글자도 필요하지 않습니다.
필요 없는 정보를 받지 않는 것이 가장 확실한 보호입니다.

| 파일 | 역할 |
|---|---|
| `static/js/mask.js` | 전송 전 브라우저에서 가림 — 이래야 네트워크를 타지 않음 |
| `core/mask.py` | 서버에서 한 번 더 가림 — JS 꺼진 경우·API 직접 호출 대비 |
| `eval/mask_eval.py` | 회귀 시험 |

가리는 항목: 주민등록번호 · 외국인등록번호 · 계좌번호 · 카드번호 · 휴대전화번호 · 이메일

**정규식은 두 파일이 같아야 합니다. 한쪽만 고치면 안내 문구와 실제 동작이 어긋납니다.**

### 왜 시험이 따로 있는가

가려야 할 것을 가리는 것보다 **건드리면 안 되는 것을 안 건드리는 쪽이 더 중요합니다.**
정규식을 느슨하게 잡으면 `2,000,000원`을 계좌번호로, `09:00~18:00`을 전화번호로 오인해
판정이 통째로 망가집니다. 개인정보를 지키려다 서비스를 부순 셈이 됩니다.

`python -m eval.mask_eval` 로 24건을 확인합니다. 금액·근로시간·계약기간·가산율·조문번호가
그대로 유지되는지 매번 봅니다. 계약서 검사 정확도에 회귀가 없는 것도 확인했습니다
(재현율 96.7% / 정밀도 100% / F1 98.3% 유지).

### 개인정보 처리방침 페이지

`GET /privacy` · `templates/privacy.html` · 푸터에 링크 추가.
계약서를 다루는 서비스에 이 페이지가 없으면 심사에서 바로 지적받습니다.

---

## 3. 문의 폼에 넣을 문구 — 그대로 복사하세요

문의 기능 자체는 문제없습니다. 위험은 **사용자가 문의창에 계약서를 통째로 붙여넣는 것**입니다.
이메일로 전달되는 구조라 한 번 나가면 우리가 삭제할 수 없습니다.
메일 서버와 받는 사람 메일함에 남습니다.

### 입력창 placeholder

```
문의 내용을 적어 주세요. 계약서 원문은 붙여넣지 말아 주세요.
```

### 입력창 아래 안내문

```
문의 내용은 이메일로 전달되므로 저희가 삭제할 수 없는 곳에 남습니다.
계약서 검토가 필요하시면 문의 대신 검사 기능을 이용해 주세요 — 그쪽은 아무것도 저장되지 않습니다.
```

### 동의 체크박스 (전송 버튼 위, 필수)

```
답변을 받기 위해 이메일 주소와 문의 내용이 수집되는 데 동의합니다. (답변 후 삭제)
```

### 개인정보 처리방침 링크

```html
<a href="/privacy">개인정보 처리방침</a>
```

### 서버 쪽 주의

- 문의 내용을 **로그에 남기지 마세요.** `print`, `app.logger.info` 로 문의 본문을 찍으면
  Render 로그에 그대로 쌓이고, 로그는 우리가 지운다고 사라지지 않습니다.
- SMTP 비밀번호는 반드시 `.env` 로 빼고 Render 환경변수에 넣으세요. 코드에 박으면
  `.gitignore` 와 무관하게 깃에 올라갑니다.
- 받는 주소는 **우리 쪽 고정 주소**여야 합니다. 사용자가 입력한 주소로 보내는 구조면
  스팸 발송 도구로 악용될 수 있습니다.

---

## 4. 결과를 이메일로 보내는 기능은 권하지 않습니다

민규 님이 물어보신 부분입니다. 마감 전까지는 넣지 않는 쪽이 낫다고 봅니다.

- 지금 `.md 내려받기` 가 이미 있고, 그건 아무것도 남기지 않습니다
- 결과에는 계약서 원문 조항이 인용문으로 들어갑니다. 원문을 빼면 이메일이 무의미하고,
  넣으면 지금까지 지킨 "아무것도 남기지 않는다"가 깨집니다
- SMTP 설정·스팸 처리·전달 실패 대응이 붙는데, 마감이 내일입니다

정 넣어야 한다면 **위반 항목과 법조문만** 보내고 계약서 원문 인용은 빼야 합니다.
그래야 메일함에 남는 것이 "제3조 위약금 조항은 근로기준법 제20조 위반" 정도가 됩니다.

---

## 5. 발표에서 쓸 한 줄

> 계약서를 저장하지 않고, 외부 API를 부르지 않으며, 주민등록번호·계좌번호는
> 브라우저를 떠나기 전에 가립니다. 지키는 것이 아니라 **처음부터 받지 않는** 쪽을 택했습니다.

주민등록번호는 고유식별정보라 법령 근거 없이는 처리 자체가 원칙적으로 금지됩니다.
우리에겐 그 근거가 없으므로, 받지 않는 것이 맞습니다.

*법률 자문이 아니며, 최종 판단은 전문가 확인이 필요합니다.*
