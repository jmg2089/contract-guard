# 프론트엔드 인계 문서

**이 문서 하나만 읽으면 프론트 작업을 시작할 수 있습니다.** 백엔드가 완성되기를 기다릴 필요 없습니다 — 아래 JSON을 하드코딩해두고 화면을 끝까지 만든 다음, 마지막에 주소만 바꿔 끼우면 됩니다.

현재 배포: https://contract-guard-uoyr.onrender.com

---

## 1. 작업 경계

| 영역 | 담당 | 파일 |
|---|---|---|
| 화면 | **프론트 담당** | `templates/`, `static/` |
| 판정 로직·데이터 | 정민규 | `core/`, `data/`, `eval/` |
| 서버 (경계선) | 둘이 합의해서만 수정 | `server.py` |

같은 파일을 둘이 건드리지 않으므로 머지 충돌이 구조적으로 나지 않습니다.

`templates/index.html`은 **백업용 최소 구현**입니다. 프론트 담당이 만든 화면으로 교체하면 됩니다. 마음껏 갈아엎으셔도 됩니다.

---

## 2. 로컬 실행

```bash
pip install -r requirements.txt
python scripts/train_classifier.py   # 분류기 학습, 10초
python server.py                     # http://localhost:5000
```

`train_classifier.py`를 안 돌려도 서버는 뜹니다. 그 경우 룰 검사만 동작하고 `학습 모델` 결과가 0건으로 나옵니다.

---

## 3. 두 가지 연동 방식 중 선택

### 방식 A — Jinja 템플릿 (지금 구조, 추천)

Flask가 HTML을 렌더링합니다. JS 프레임워크 없이 만들 때 가장 빠릅니다.

`server.py`의 `index()` 가 템플릿에 넘기는 변수:

```python
{
  "min_wage": 10320,          # int
  "min_wage_year": 2026,      # int
  "law_count": 28,            # int  탑재된 법조문 수
  "unverified": 0,            # int  원문 미검증 조문 수 (0이어야 정상)
  "text": "...",              # str  사용자가 입력한 원문 (재표시용)
  "result": { ... }           # dict 또는 None — 아래 4번 스키마
}
```

폼은 `POST /` 로 보냅니다.

```html
<form method="post">
  <textarea name="text"></textarea>
  <input type="checkbox" name="use_llm">   <!-- 선택. 체크 안 하면 룰+분류기만 -->
  <button type="submit">검사하기</button>
</form>
```

### 방식 B — JS로 API 호출

React/Vue 등으로 따로 만들 때. CORS는 이미 열려 있습니다.

```js
const res = await fetch("http://localhost:5000/api/analyze", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ text: contractText, use_llm: false }),
});
const data = await res.json();   // 아래 4번 스키마
```

---

## 4. 응답 스키마 — 이것만 알면 됩니다

```jsonc
{
  "meta": {
    "clause_count": 4,        // 검토한 조항 수
    "violation_count": 2,     // 위반 소지
    "unfavorable_count": 2,   // 불리 조항 / 확인 권장
    "rule_findings": 0,       // 룰 검사가 잡은 건수
    "model_findings": 4,      // 학습 모델이 잡은 건수
    "llm_findings": 0,        // LLM이 잡은 건수 (기본 0)
    "blocked_by_gate": 0,     // 인용 검증에서 차단된 건수  ★ 반드시 노출
    "llm_enabled": false
  },

  "clauses": [                // 분할된 조항 전체 (문제없는 것 포함)
    {
      "id": "c1",
      "article_no": 5,        // 제5조. 번호 없는 계약서면 null
      "title": "교육비",
      "text": "제5조(교육비) 근로자가 입사 후 2년 이내 퇴사하면...",
      "start": 0, "end": 52   // 원문 내 문자 오프셋 (하이라이트용)
    }
  ],

  "findings": [               // 문제가 발견된 조항. severity 순 정렬됨
    {
      "clause_id": "c1",
      "severity": "violation",          // violation | unfavorable
      "severity_label": "위반 소지",      // 그대로 화면에 써도 되는 한글
      "clause_title": "제5조 교육비",
      "clause_text": "제5조(교육비) 근로자가 입사 후 2년 이내 퇴사하면...",
      "law_id": "근로기준법-제20조",
      "law_label": "근로기준법 제20조(위약 예정의 금지)",
      "quote": "사용자는 근로계약 불이행에 대한 위약금 또는 손해배상액을 예정하는 계약을 체결하지 못한다.",
      "reason": "퇴사나 계약 불이행을 이유로 금전 부담을 지우는 조항입니다. ...",
      "suggestion": "해당 조항을 삭제하고, 실제 발생한 손해가 있을 때 개별적으로 청구하도록 바꾸십시오.",
      "source": "model",                // rule | model | llm
      "source_label": "학습 모델",        // 그대로 써도 되는 한글
      "gate_passed": true
    }
  ],

  "dropped": [                // 인용 검증에서 걸러진 판정 (보통 빈 배열)
    { "clause_id": "c3", "claimed_law_id": "", "claimed_quote": "" }
  ]
}
```

### 다른 엔드포인트

```
GET /health      상태 확인. 분류기 로드 여부, 법조문 수, 최저임금 기준
GET /api/laws    탑재된 법조문 전체 (근거 조문 전문을 모달로 띄울 때)
```

---

## 5. 화면에 반드시 남겨야 하는 것 3가지

디자인은 자유지만 **이 셋은 심사 점수와 직결**됩니다. 빼지 말아주세요.

### ① `meta.blocked_by_gate` — "인용 검증에서 차단 N건"

지표 카드로 크게 노출해주세요. 이게 데모 영상의 핵심 장면입니다.

이 시스템은 **법 문장을 생성하지 않습니다.** 근거 문장은 언제나 법령 원문에서 그대로 인용되고, 원문과 일치하지 않는 판정은 사용자에게 도달하기 전에 폐기됩니다. 이 숫자가 그 장치가 작동한 횟수입니다.

### ② `quote` — 근거 법조문 원문

판정마다 반드시 함께 보여주세요. **"왜 위반인가"를 우리가 설명하는 게 아니라 법이 말하게 하는 것**이 이 서비스의 핵심입니다. 인용문은 한 글자도 가공하지 말고 그대로 출력해주세요.

### ③ `source_label` — 룰 검사 / 학습 모델 구분

어느 층이 잡았는지 배지로 표시해주세요.

숫자 판단(최저임금, 근로시간, 해고예고 일수)은 결정론적 룰 22개가, 의미 판별(일방적 근로조건 변경, 사실상 강제근로)은 학습 분류기가 담당합니다. 이 구분이 보여야 "왜 두 층을 만들었는가"에 답이 됩니다.

---

## 6. 디자인 가이드

- **severity 색상**: `violation` 빨강 계열 / `unfavorable` 주황 계열. 현재 `#b3261e` / `#8a6100`
- **조항 원문과 법령 인용문을 시각적으로 구분**해주세요. 둘 다 인용문처럼 보이면 무엇이 계약서고 무엇이 법인지 헷갈립니다
- **모바일 대응 필수.** 심사위원이 휴대폰으로 열어볼 가능성이 높습니다
- **`findings`가 빈 배열일 때** "문제 없음" 화면을 꼭 만들어주세요. 정상 계약서를 넣었을 때 아무것도 안 나오면 고장난 것처럼 보입니다
- **첫 로딩 30초** — 무료 인스턴스는 15분 무요청 시 잠듭니다. 로딩 스피너에 "서버를 깨우는 중입니다(최대 30초)" 안내가 있으면 좋습니다
- 하단 면책 문구는 그대로 유지: *"이 도구는 법률 자문이 아닙니다..."*

---

## 7. 테스트용 계약서 2종

화면 확인용입니다. 두 경우의 결과가 확연히 다릅니다.

### A. 룰 검사가 잡는 계약서 (rule 4건 / model 0건)

```
제2조(수습기간) 수습기간은 6개월로 하며 수습기간 중 임금은 80%를 지급한다.
제3조(임금) 임금은 시급 9,500원으로 한다.
제5조(손해배상) 근로자가 계약기간 만료 전 퇴사하는 경우 위약금으로 금 3,000,000원을 지급한다.
제9조(퇴직) 근로자는 회사의 사전 서면 동의 없이 퇴사할 수 없다.
```

### B. 학습 모델이 잡는 계약서 (rule 0건 / model 4건)

```
제1조(근로조건) 회사는 근로자의 동의 없이 임금 및 근로조건을 변경할 수 있다.

제2조(임금 유예) 회사의 경영 사정이 악화된 경우 임금 지급을 무기한 유예할 수 있다.

제5조(교육비) 근로자가 입사 후 2년 이내 퇴사하면 회사가 지원한 어학연수 비용을 전액 돌려주어야 한다.

제6조(업무지시) 근로자는 회사가 지시하는 어떠한 업무도 거부할 수 없으며 거부 시 계약 위반으로 본다.
```

### C. 정상 계약서 (아무것도 안 잡혀야 정상)

```
제1조(계약기간) 본 계약은 2026년 10월 1일부터 기간의 정함이 없는 것으로 한다.
제2조(임금) 임금은 월 2,800,000원으로 하며 매월 25일에 통화로 전액 지급한다.
제3조(근로시간) 소정근로시간은 1일 8시간, 1주 40시간으로 하며 휴게시간 1시간을 부여한다.
제4조(연차) 연차 유급휴가는 근로자가 청구한 시기에 부여한다.
```

---

## 8. 배포

`main` 브랜치에 push하면 Render가 자동으로 다시 빌드합니다. 별도 작업 없습니다.

```bash
git add .
git commit -m "프론트 개선"
git push
```

빌드 3~5분. 실패하면 Render 대시보드 `Logs` 탭을 확인하세요.

---

## 9. 막히면

- `/health`가 200을 주는지 먼저 확인 — 서버 자체가 죽었는지 판별됩니다
- `classifier.loaded`가 `false`면 분류기 미학습 상태. 룰 검사만 동작합니다
- `500` 에러는 백엔드 쪽이니 정민규에게 조항 원문과 함께 알려주세요
- CORS 에러가 나면 `server.py`의 `CORS(app)` 설정을 확인하세요 (기본은 전체 허용)
