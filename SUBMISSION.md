# 제출 문서 — 그대로 복사해서 쓰세요

대회 제출 항목은 **배포 링크 / 해결하려는 문제 / AI 활용 방식 / 사용한 AI 툴** 네 가지다.
예선 채점은 기획력 · 실현가능성 · 확장성 · **AI 활용의 적절성**으로 이뤄진다.

---

## 배포 링크

```
https://contract-guard-uoyr.onrender.com
```

무료 인스턴스 특성상 첫 접속 시 서버 기동에 30초가 소요될 수 있습니다.
시스템 상태는 `/health` 에서 확인할 수 있습니다. (분류기 로드 여부, 탑재 조문 수, 적용 최저임금 기준)

---

## 해결하려는 문제

근로계약서에는 법적으로 무효인 조항이 흔하게 들어간다. 퇴사 시 위약금, 최저임금 미달, 수습기간 과다, 가산수당 배제, 퇴직금 분할 지급 같은 것들이다. 문제는 대부분의 근로자가 **그 조항이 위법이라는 사실 자체를 모른 채 서명한다**는 데 있다.

기존 방법은 둘 다 막혀 있다. 노무사 상담은 비용과 시간이 들어 계약서 한 장 때문에 찾아가기 어렵고, 인터넷 검색은 내 계약서의 그 문장이 어느 조문에 걸리는지까지는 알려주지 않는다.

이 서비스는 계약서를 붙여넣으면 조항별로 위반·불리 여부를 판정하고 **근거가 되는 법령 조문을 원문 그대로 인용해서** 보여준다. 전체 위험도를 0~100점으로 요약하고, 문제 구간을 원문 위에 표시하며, 수정 제안을 담은 **검토 의견서를 파일로 내려받아 협상 자료로 쓸 수 있다.**

계약서는 PDF·스캔본·사진·캡처 화면·텍스트 붙여넣기를 모두 받는다. 글자가 이미지로 들어 있는 경우 **브라우저 안에서 OCR로 읽어낸다 — 파일이 서버로 전송되지 않는다.**

계약서를 손에 들고 있지 않은 방문자를 위해 샘플 계약서 4종을 버튼으로 제공하며, 같은 계약서를 PDF로도 내려받아 업로드 경로까지 확인할 수 있다.

---

## AI 활용 방식

**외부 LLM API를 사용하지 않았습니다.**

근로기준법 조문과 실제 분쟁 유형을 분석해 학습 데이터 539문장(15개 클래스)을 직접 구축하고, TF-IDF 문자 n-gram 기반 분류기를 학습시켰습니다. 3-fold 교차검증 정확도 77.6%입니다.

### 2층 구조 — 역할을 나눈 이유

| 층 | 담당 | 방식 |
|---|---|---|
| 1층 결정론적 룰 26개 | 숫자로 확정 판단되는 항목 (최저임금 미달, 근로시간 초과, 해고예고 일수, 수습기간) | 정규식 + 산술 비교 |
| 2층 학습 분류기 | 의미를 읽어야 하는 항목 (일방적 근로조건 변경, 임금 지급 유예, 사실상 강제근로) | TF-IDF + LinearSVC |

**시급 9,500원이 10,320원보다 작은지를 언어 모델에게 물어볼 이유가 없습니다.** 파이썬 비교 연산자 하나면 되고, 그쪽이 더 빠르고 틀리지 않습니다. 숫자 비교는 언어 모델이 가장 자주 틀리는 영역이기도 합니다.

반대로 "회사가 지원한 어학연수 비용을 전액 돌려주어야 한다"는 정규식으로 잡을 수 없습니다. '위약금'이라는 단어가 없지만 근로기준법 제20조가 금지하는 위약 예정에 해당하기 때문입니다. 이런 조항이 분류기의 자리입니다.

실제로 측정했습니다.

| 계약서 유형 | 룰 검사 | 학습 모델 |
|---|---|---|
| 숫자·명시 문구 중심 | **4건 탐지** | 0건 |
| 의미 판별 필요 | 0건 | **4건 탐지** |

한 층만으로는 절반밖에 잡지 못합니다.

### 할루시네이션이 구조적으로 불가능한 설계

이 시스템은 **법 문장을 생성하지 않습니다.**

분류기는 "어느 조문 유형에 해당하는가"만 선택합니다. 사용자에게 보여주는 근거 문장은 언제나 법제처 국가법령정보센터 API로 받아온 조문 원문에서 그대로 발췌됩니다. 모델이 법 문구를 만들어낼 경로 자체가 존재하지 않습니다.

여기에 더해, LLM을 선택적으로 연결할 경우를 대비한 **인용 검증 게이트**를 구현했습니다. 판정 결과에 조문 ID와 원문 인용구를 강제하고, 인용구가 검색된 조문 텍스트의 실제 부분문자열이 아니면 그 판정을 코드 레벨에서 폐기합니다. 차단된 건수는 화면에 그대로 표시됩니다.

### 확신도에 따라 말하는 강도를 바꿉니다

법률 도구에서 잘못된 단정은 신뢰를 깎습니다. 그래서 분류기 확신도가 60% 이상이면 "위반 소지", 50~60%면 "확인이 필요합니다"로 표현을 낮춥니다. 50% 미만이면 아무 말도 하지 않습니다.

임계값은 0.30~0.70 전 구간을 측정해 결정했습니다.

```
0.45 → 재현율 96.7% / 정밀도 90.6%
0.50 → 재현율 96.7% / 정밀도 100.0%   ← 채택
0.60 → 재현율 90.0% / 정밀도 100.0%
```

같은 이유로 문맥이 없는 짧은 조항(25자 미만)은 분류기가 판정하지 않습니다. "연차 | 근로기준법에 따름" 같은 표 형식 항목을 단어 하나만 보고 오판하던 문제를 이렇게 막았습니다. 숫자를 보는 룰은 짧은 조항에서도 정확하므로 그쪽이 담당합니다.

### 성능

자체 구축 평가셋 11건 / 라벨 30개 / **대조군 3건** 기준입니다. 법조문 37개, 결정론적 룰 26개를 탑재한 상태입니다. 대조군이 없으면 "아무거나 위반이라고 답하는 모델"도 만점이 나오므로, 정상 조항만 있는 계약서를 반드시 포함시켰습니다.

| 구성 | 재현율 | 정밀도 | F1 |
|---|---|---|---|
| 룰만 | 86.7% | 100% | 92.9% |
| **룰 + 학습 분류기** | **96.7%** | **100%** | **98.3%** |

정답 판정 기준은 **(조항 번호, 근거 법조문 ID) 동시 일치**입니다. 결론이 맞아도 근거 조문이 틀리면 오답으로 셉니다.

별도로 적대적 테스트 9건을 운영합니다. "회사는 위약금을 청구하지 **아니한다**" 같이 위험 단어가 들어갔지만 근로자에게 유리한 조항을 오탐하는지 검사하는 케이스입니다. 이 과정에서 한국어 부정 활용형 처리 버그와 짧은 조항이 검사에서 누락되는 버그를 찾아 수정했습니다.

### 우리가 만들지 않은 문서로 검증했습니다

자체 제작 샘플로만 시연하면 "그 샘플에 맞춰 튜닝했다"는 지적을 반박할 수 없습니다. 그래서 **고용노동부 표준근로계약서**를 샘플에 포함했습니다. 정부가 배포하는 공식 서식에 값만 채운 것으로, 저희가 지어낸 문장이 하나도 없습니다.

넣자마자 오탐이 나왔습니다. **"연차유급휴가는 근로기준법에서 정하는 바에 따라 부여함"** — 정부 서식 원문을 연차 위반으로 판정했습니다. 실제 문서로 시험하지 않았으면 발견하지 못했을 버그입니다. "법령이 정하는 바에 따라" 계열 문구 18개를 학습 데이터에 추가해 해결했고, 현재 이 문서에서 탐지 0건입니다.

### 판정을 쓸 수 있는 형태로 바꾸는 층

판정 목록만으로는 사용자가 무엇을 해야 할지 알 수 없습니다. 세 가지를 덧붙였습니다.

- **위험도 점수** — 위반 소지 1건당 25점, 불리 조항 1건당 10점, 100점 상한. **산식을 화면에 그대로 노출합니다.** 근거 없는 숫자는 신뢰를 얻지 못하기 때문입니다. 조항 수로 나누지 않았습니다 — 위약금 조항 하나가 있는 계약서는 조항이 5개든 50개든 똑같이 위험합니다
- **원문 하이라이트** — 조항 분할 단계에서 이미 문자 오프셋을 들고 있어 추가 계산 없이 문제 구간을 원문 위에 표시합니다
- **PDF·스캔본·사진 입력** — 글자가 들어 있는 PDF는 서버가 즉시 추출하고, 스캔본과 사진은 `tesseract.js` 로 **사용자 브라우저 안에서** 읽습니다. 서버에서 OCR을 돌리려면 무료 인스턴스에 설치할 수 없는 엔진이 필요하거나 외부 API를 불러야 하는데, 브라우저에서 처리하면 둘 다 피하면서 **계약서 이미지가 서버로 전송되지 않는다**는 장점까지 생깁니다. 근로계약서는 이름·주소·급여가 담긴 민감 문서이므로 이는 부수 효과가 아니라 설계 판단입니다. OCR 결과는 바로 분석하지 않고 입력창에 채워 사용자가 확인·수정한 뒤 실행하게 했습니다 — 잘못 읽은 글자로 위반 판정을 내리면 신뢰를 잃기 때문입니다
- **검토 의견서 내려받기** — 원문을 고치지 않습니다. 조항 아래에 문제·근거 조문·수정 제안을 덧붙인 마크다운 파일입니다. 계약서는 당사자가 합의해서 고치는 문서이므로, 임의로 바꾼 것처럼 보이면 안 됩니다. 협상 자료로 쓰는 형태가 맞습니다

### 운영 특성

- 외부 API 호출 0회 / 비용 0원 / 네트워크 의존 없음
- 최대 메모리 155MB, 조항 1건 추론 2.4ms
- 같은 입력에 항상 같은 출력 (온도 파라미터 없음)
- 분류기는 배포 시 서버에서 직접 학습 — 모델 파일 버전 불일치 문제 원천 차단
- 분류기 로드에 실패해도 룰 검사 26개로 서비스 계속 동작 (재현율 86.7%)

---

## 사용한 AI 툴

| 구분 | 내용 |
|---|---|
| 학습 모델 | scikit-learn — TF-IDF(char_wb 2-4gram) + LinearSVC + CalibratedClassifierCV |
| 학습 데이터 | 직접 구축 539문장 / 15개 클래스 + 법령 원문 자동 편입 |
| 검색 | rank-bm25 — 조항별 관련 법조문 검색 |
| 법령 데이터 | 법제처 국가법령정보센터 Open API (근로기준법·최저임금법·근로자퇴직급여보장법·남녀고용평등법·기간제법 37개 조문, 전부 원문 검증) |
| 백엔드 | Flask + gunicorn |
| 배포 | Render (무료 인스턴스) |
| 개발 보조 | Claude — 설계 검토, 코드 작성, 학습 데이터 초안 작성에 활용. **배포된 서비스는 어떠한 LLM API도 호출하지 않습니다.** |

---

## 확장 계획

파이프라인은 근거 법령 데이터(`data/laws.json`)와 학습 데이터만 교체하면 그대로 작동합니다. 구조를 바꿀 필요가 없습니다.

1. **계약 유형 확장** — 용역계약, 프리랜서 계약, 임대차계약, 서비스 약관
2. **B2B** — 채용 플랫폼·HR SaaS에 계약서 사전 검토 기능으로 임베드. 기업 입장에서는 위법 조항으로 인한 분쟁 비용을 사전에 차단하는 가치
3. **데이터 축적** — 사용자가 검토한 조항 유형 분포 자체가 "어떤 독소조항이 실제로 유통되는가"라는 공개 자료가 됨

---

## 다음 단계

현재 구조는 근거 법령 데이터와 학습 데이터만 교체하면 그대로 확장됩니다. 파이프라인 코드를 바꿀 필요가 없도록 설계했습니다.

| 단계 | 내용 | 예상 효과 |
|---|---|---|
| 조문 확대 | 37개 → 근로기준법 전 조문 및 시행령 | 탐지 가능 유형 확대 |
| 학습 데이터 확대 | 클래스당 35개 → 50개 | 분류 정확도 83% 이상 (학습 곡선 실측 기반 추정) |
| OCR 정확도 개선 | 전처리(기울기 보정·이진화) 추가 | 스캔 상태가 나쁜 문서 인식률 향상 |
| 계약 유형 확대 | 용역·프리랜서·임대차·서비스 약관 | 적용 시장 확대 |

학습 데이터를 얼마나 늘려야 하는지는 추측이 아니라 측정으로 정했습니다. 클래스당 문장 수를 3개부터 8개까지 줄여가며 정확도를 재고(45.1% → 61.0%), 그 곡선을 외삽해 20개에서 71%, 40개에서 80%라는 목표치를 도출했습니다. 모델 종류(LinearSVC / LogisticRegression / NaiveBayes / SGD)와 특징 추출 방식(문자 n-gram / 어절 n-gram / 결합)을 각각 바꿔 측정한 결과 모두 57~63% 구간에 머물렀기 때문에, 병목이 모델이 아니라 데이터임을 먼저 확인한 뒤 내린 결론입니다.

---

## 참고

이 도구는 법률 자문이 아닙니다. 참고용 검토 결과이며, 실제 분쟁이나 계약 체결 판단은 노무사·변호사 또는 고용노동부 상담(1350)을 통해 확인하시기 바랍니다. 이 문구는 서비스 화면 하단에도 상시 표시됩니다.
