# 실행 계획 — 9/18(금) 밤 ~ 9/20(일) 제출

## 원칙 3개

1. **토요일 밤까지 배포 링크가 살아있어야 한다.** 못생겨도 좋다. "작동하지 않는 제출물은 실격"이 이 대회의 명시된 규칙이다. 일요일에 처음 배포하는 팀이 매년 죽는다.
2. **기능은 1개다.** 업로드 → 조항별 판정 + 근거 인용. 로그인, 히스토리 저장, 계약서 종류 확장, 다국어, PDF 리포트 출력 — 전부 버린다. 발표에서 "확장 계획"으로 말하면 된다. 확장성은 **말로** 평가받는 항목이다.
3. **인터페이스를 먼저 얼린다.** `core/types.py`의 `Clause` / `LawArticle` / `Finding`. 이게 고정돼야 두 사람이 서로를 기다리지 않는다.

---

## 역할 분담

팀원이 기술적으로 더 강하다면 **파이프라인 코어를 통째로 넘기고**, 팀장은 파이프라인에 들어갈 재료와 평가·발표를 맡는다. 같은 파일을 둘이 건드리는 순간 머지 충돌로 반나절이 날아간다.

### 팀원 — 파이프라인 코어
- `core/segment.py` 조항 분할 정확도 개선 (표 형식, 별지, 붙임 처리)
- `core/retrieve.py` 검색 품질 — 동의어 사전 확장이 가장 효율 높다
- `core/judge.py` 프롬프트 튜닝 + 인용 게이트
- `core/pipeline.py`, `app.py`, 배포

### 정민규(팀장) — 재료 · 검증 · 발표
- `data/laws.json` 을 법제처 **공식 원문**으로 교체 (`scripts/fetch_laws.py`)
- `eval/dataset.json` 계약서 10건 / 라벨 30개 제작
- `core/rules.py` 룰 추가 및 정규식 정밀화
- 평가 실행 → 발표에 쓸 숫자 확정
- 제출 문서 + 데모 영상 + 발표 자료

여기에 더해 팀장의 진짜 일은 **자르는 것**이다. 이틀짜리 프로젝트는 실력 부족이 아니라 범위 초과로 망한다. 팀원이 "이것만 추가하면"이라고 할 때 "일요일 오후에 시간 남으면"이라고 답하는 사람이 필요하다.

---

## 시간표

| 구간 | 팀원 | 정민규 | 끝났을 때 상태 |
|---|---|---|---|
| **금 밤 3h** | 리포 clone, 로컬 구동, `types.py` 합의 | 법제처 OC 발급, `fetch_laws.py` 실행 | 둘 다 `python -m eval.run_eval --no-llm` 통과 |
| **토 오전 4h** | 조항 분할 개선, 검색 동의어 확장 | 계약서 샘플 5건 + 라벨 15개 | 실제 계약서 PDF 1건이 조항 단위로 쪼개짐 |
| **토 오후 4h** | LLM 판정 + 게이트 연결, 실제 키로 동작 | 룰 5개 추가, 평가 1차 실행 | 파이프라인 end-to-end 동작 |
| **토 밤 3h** | **Streamlit Cloud 배포** ← 절대 미루지 않음 | 계약서 5건 추가 (대조군 2건 포함) | **배포 URL 확보** |
| **일 오전 4h** | 프롬프트 튜닝, 게이트 차단 로그 확인 | 평가 최종 실행, 숫자 확정 | 재현율·정밀도·차단 건수 확정 |
| **일 오후 4h** | UI 정리, 샘플 계약서 버튼 추가 | 제출 문서 + 데모 영상 녹화 | 제출 직전 상태 |
| **일 저녁** | 배포 URL 최종 확인 | 제출 | 완료 |

---

## 제출 문서에 반드시 들어갈 것

대회가 요구하는 항목은 **배포 링크 / 해결하려는 문제 / AI 활용 방식 / 사용한 AI 툴** 네 가지다. 예선 채점은 기획력 · 실현가능성 · 확장성 · **AI 활용의 적절성**으로 이뤄진다.

**AI 활용 방식** 칸이 승부처다. 여기에 이렇게 쓴다.

> LLM에 법률 지식을 묻지 않는다. 조항마다 법령 DB에서 관련 조문을 검색해 그것만 컨텍스트로 주고, 판정 결과에 조문 ID와 원문 인용구를 강제한다. 인용구가 검색된 조문의 실제 부분문자열이 아니면 그 판정은 코드 레벨에서 폐기된다. 자체 평가셋 10건·라벨 30개 기준 재현율 N%, 정밀도 N%, 인용 검증에서 차단된 판정 N건.
>
> 숫자 비교(최저임금 미달, 수습기간 초과, 연차 일수)는 언어 모델이 가장 자주 틀리는 영역이므로 결정론적 룰로 분리했다. LLM은 문장의 의미를 읽어야 하는 조항에만 쓴다.

**확장성** 칸: 용역계약 → 프리랜서 계약 → 임대차 → 서비스 약관. 근거 법령 JSON만 교체하면 파이프라인은 그대로다. 실제로 그렇게 설계돼 있으니 거짓말이 아니다.

---

## 데모 영상 (60초 이내)

1. 독소조항이 박힌 계약서 PDF 업로드 (0~10초)
2. 빨간 카드 4개가 뜨고, 각각 근거 조문이 인용돼 있음 (10~35초)
3. **"인용 검증에서 차단 N건" 지표를 가리키며** — 모델이 지어낸 근거를 시스템이 걸러낸 횟수 (35~50초)
4. 평가 스크립트 출력 화면 — 재현율/정밀도 (50~60초)

3번이 다른 팀 영상에는 없는 장면이다. 여기에 시간을 써라.

---

## 하지 말 것

- 스캔 PDF OCR — 범위 밖. "텍스트 붙여넣기" 탭으로 우회한다.
- 임베딩 모델 런타임 로딩 — Streamlit 무료 티어 메모리에서 터진다.
- 법령 전체 크롤링 — 조문 20개면 충분하다. 데모에 쓸 조항 종류가 그만큼이다.
- 회원가입/DB — 심사위원은 로그인하지 않는다.
- 일요일에 첫 배포.
