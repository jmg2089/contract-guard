# 정민규 작업 목록 — 순서대로, 위에서부터

팀원은 `templates/`만 건드린다. 당신은 `core/`, `data/`, `eval/`만 건드린다. 겹치는 파일이 없으므로 머지 충돌이 나지 않는다.

---

## STEP 0 — 환경 확인 (15분)

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python -m eval.run_eval --no-llm -v
```

표가 뜨면 끝. 그다음 `python server.py` → `http://localhost:5000` 에서 화면이 뜨는지 확인.

**막히면 여기서 멈추고 에러를 그대로 복사해 보낼 것.** 이 단계에서 30분 이상 쓰지 않는다.

---

## STEP 1 — 법령 원문 교체 (오늘 밤, 최우선)

지금 `data/laws.json`의 18개 조문은 전부 `"verified": false`다.

**왜 이게 1번인가.** 인용 게이트는 "모델이 인용한 문장이 이 JSON의 `text` 안에 실제로 있는가"로 판정한다. 그래서 `text`가 공식 원문과 글자 하나라도 다르면 **맞는 판정까지 전부 차단된다.** 이 파일이 틀리면 나머지 작업이 전부 헛돈다.

### 방법 A — API (권장)

1. https://open.law.go.kr 회원가입 → OC 즉시 발급 (심사 없음)
2. ```powershell
   $env:LAW_OC="발급받은ID"
   python scripts/fetch_laws.py
   ```

### 방법 B — 손으로 (A가 막히면 30분이면 된다)

1. https://law.go.kr 에서 근로기준법 검색
2. 조문 본문을 복사해 `data/laws.json`의 `text`에 붙여넣기
3. `"verified"`를 `true`로 변경

붙여넣을 때 주의: 조문 번호(`제20조(위약 예정의 금지)`)는 `text`에 넣지 않는다. 본문만 넣는다. 게이트는 공백과 구두점 차이는 무시하므로 줄바꿈은 신경 쓰지 않아도 된다.

### 검증

```
python -m eval.run_eval --no-llm -v
```

교체 후에도 재현율이 그대로면 성공. **떨어졌다면 `rules.py`가 인용하는 문장이 새 원문에 없다는 뜻이다.** 그건 버그가 아니라 게이트가 제대로 일하고 있다는 증거다. `core/rules.py`의 `_finding()` 호출에서 `sentence_idx` 값을 조정하면 된다.

---

## STEP 2 — 조문 수 늘리기 (토요일 오전, 18개 → 40개)

없는 조문은 검색될 수 없다. 법 전체를 넣을 필요는 없다. **데모에 쓸 계약서에 나올 조항 종류를 먼저 세고, 그걸 덮는 조문만 채운다.**

추가 후보:
- 근로기준법 제2조(정의), 제6조(균등처우), 제27조(해고사유 서면통지), 제57조(보상휴가), 제93조(취업규칙)
- 기간제 및 단시간근로자 보호 등에 관한 법률 제4조(기간제근로자 사용기간), 제8조(차별적 처우 금지)
- 남녀고용평등법 제18조의2(배우자 출산휴가), 제19조(육아휴직)
- 산업안전보건법 제5조(사업주의 의무)

`data/laws.json`에 같은 형식으로 추가하기만 하면 검색 인덱스는 자동으로 다시 만들어진다.

---

## STEP 3 — 평가셋 확장 (토요일 하루, 가장 오래 걸림)

현재 3건 / 라벨 8개다. **목표는 10건 / 라벨 30개.**

지금 숫자(재현율 100%)는 발표에 쓰면 안 된다. 케이스 3개에 룰을 맞춰 짠 거라 아무 의미가 없다.

### 만드는 법

1. 고용노동부 표준근로계약서 양식을 받는다 (`moel.go.kr` 검색)
2. 조항을 하나씩 비튼다 — 최저임금 아래로, 수습 6개월로, 위약금 추가, 연차 10일로
3. `eval/dataset.json`에 `text`와 `labels`를 넣는다
4. 라벨은 `{"article_no": 5, "law_id": "근로기준법-제20조", "severity": "violation", "tag": "위약 예정"}` 형식

### 반드시 지킬 것

- **정상 조항만 있는 대조군 2건 이상.** 없으면 정밀도가 거짓말을 한다. 독소조항만 넣은 계약서로 측정하면 "아무거나 위반이라고 답하는 모델"도 100%가 나온다.
- **룰로는 못 잡고 의미를 읽어야만 잡히는 케이스 5건 이상.** 예: "근로자는 회사의 사전 서면 동의 없이 퇴사할 수 없다", "회사가 정하는 바에 따라 근로시간을 조정할 수 있으며 이에 대한 보상은 별도로 정하지 않는다". 이런 게 없으면 심사위원의 "룰만 쓰면 되는 거 아닌가요"에 답할 수 없다.

---

## STEP 4 — 룰 추가 (토요일 오후, 12개 → 20개)

`core/rules.py`에 함수를 하나 만들고 맨 아래 `RULES` 리스트에 이름을 넣으면 끝이다.

```python
def r_my_rule(c: Clause):
    if re.search(r"패턴", c.text):
        return _finding(c, "violation", "근로기준법-제N조", 0,
                        "왜 문제인지 2문장 이내",
                        "대체 문구 제안")
```

`_finding()`이 법령 원문에서 문장을 알아서 떠오므로 인용 게이트는 자동으로 통과한다.

추가 후보: 경업금지 기간 과도(2년 초과), 해고사유 서면통지 배제, 육아휴직 제한, 기간제 2년 초과, 임금 비밀유지 강요, 취업규칙 일방 변경, 연차 사용 시기 강제.

---

## STEP 5 — 숫자 확정 (일요일 오전)

```
python -m eval.run_eval -v          # LLM 붙여서
python -m eval.run_eval --no-llm -v # 룰만
```

**두 숫자를 다 기록한다.** 이게 발표의 핵심 슬라이드가 된다.

```
룰만        재현율 62%  정밀도 100%
룰 + LLM    재현율 89%  정밀도 94%   ← LLM이 27%p를 더 잡았다
인용 게이트에서 차단된 판정   7건
```

이 표 하나가 "AI를 왜 썼는가"와 "AI를 어떻게 통제했는가"를 동시에 답한다. 슬라이드 다섯 장보다 강하다.

---

## STEP 6 — 제출 문서 (일요일 오후)

대회 제출 항목은 **배포 링크 / 해결하려는 문제 / AI 활용 방식 / 사용한 AI 툴** 네 가지다. `PLAN.md`에 `AI 활용 방식` 초안이 들어있다.

---

## 하지 말 것

- 임베딩 모델 추가 — Render 무료 512MB에서 터진다. BM25로 충분하다
- 스캔 PDF OCR — 범위 밖
- DB(PostgreSQL 등) 붙이기 — 조문 40개는 JSON 파일이 맞다. DB는 배포만 복잡하게 만든다
- `core/types.py` 수정 — 팀원과 합의 없이는 절대
- 일요일에 첫 배포
