# 파이썬 3.11 또는 3.12 에서 설치할 것.
# 배포(Render)는 render.yaml / runtime.txt 로 3.11.9 에 고정돼 있다.
#
# scikit-learn 은 버전을 고정하지 않는다. 특정 버전으로 핀하면 그 버전의 윈도우 빌드본이
# 없는 파이썬에서 소스 컴파일로 넘어가 C 컴파일러를 요구하며 실패한다.
# 하한만 두고 나머지는 환경에 맡기는 편이 안전하다.

flask>=3.0,<4
flask-cors>=5.0
gunicorn>=23.0
rank-bm25>=0.2.2
python-dotenv>=1.0

# 조항 유형 분류기. 외부 API 없이 '의미'를 판별하는 층이다.
scikit-learn>=1.3
joblib>=1.3

# 선택 — LLM 판정을 추가로 붙일 때만 필요하다. 없어도 전체가 동작한다.
# openai>=1.40

# PDF 업로드. 텍스트 레이어가 있는 PDF에서 글자를 뽑는다.
pdfplumber>=0.11

# 두 번째 추출 엔진. pdfplumber(pdfminer) 와 구현이 달라, 한쪽이 못 읽는 PDF를
# 다른 쪽이 읽는 경우가 있다. core/pdftext.py 가 둘을 모두 돌려 나은 쪽을 고른다.
pypdf>=4.0

# 마스킹 미리보기용. 둘 다 pdfplumber 가 이미 끌고 오지만 여기서 직접 import 하므로
# 명시해 둔다. 남의 의존성에 얹혀 쓰다가 그쪽이 버전을 올리면 조용히 깨진다.
#   pypdfium2 : PDF 페이지를 이미지로 렌더 (BSD/Apache. AGPL 인 PyMuPDF 를 피한 이유)
#   pillow    : 렌더된 이미지 위에 검은 박스를 그린다
pypdfium2>=4.0
pillow>=10.0

# 샘플 PDF 생성용(개발 전용). 배포에는 필요 없지만 빌드에 부담이 없어 함께 둔다.
reportlab>=4.0
